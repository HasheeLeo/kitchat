const React = require("react-native");

const { StyleSheet } = React;

export default {

inputTextStyle: {
	paddingBottom: 20,
	fontWeight: '400',
	height: height / 13,
	paddingLeft: width / 40,
	color: mainThemeColor,
	fontSize: 20,
},
  
};